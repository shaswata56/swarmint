# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Virtual env (swarmint's pinned deps live in pyproject / requirements)
.venv/
venv/
env/

# Runtime artifacts / outputs
results.csv
multiproc_metrics/
*.dhtport
scratch_net_probe/

# Generated by docker/nat-test/gen_compose.py (derived; regenerate with the script)
docker/nat-test/docker-compose.yml

# Datasets / caches (fetched on demand; not vendored)
scikit_learn_data/
*.npz

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# JS client (js/swarmint-client): deps installed via `npm install`, not vendored.
# web/vendor/ IS committed — those are the browser-ready copies infer.html loads
# directly with no build step, deliberately checked in for a zero-install page.
js/swarmint-client/node_modules/
js/swarmint-client/package-lock.json
